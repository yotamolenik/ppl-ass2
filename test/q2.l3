(define last-element
   (lambda (lst)
      @TODO
  )
)

(define power
    (lambda (n1 n2)
       @TODO
       if(= n2 1)
         return n1
      return (* n1 power(n1,(- n2 1)))
    )
)

(define sum-lst-power
    (lambda (lst n)
       @TODO
    )
)
      
  
(define num-from-digits
  (lambda (lst)
     @TODO
  )
)

(define is-narcissistic
  (lambda (lst)
       @TODO
    )
)
 
